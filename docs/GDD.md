# Glorp Battle — прототип боевой системы (MVP)

**Гейм-дизайн-документ и техническое задание для разработчика**
Версия 2.3 · 2026-09-18 · на основе `battle_network_mvp_gdd.pdf` (v1)

> **Интерфейс заменяется физическим терминалом NET-01 — см. [TERMINAL.md](TERMINAL.md)** [решение 2026-09-17]. Правила боя остаются здесь; управление и отображение (§7.6, §12, §13, §14) по мере этапов T1–T2 описывает TERMINAL.md, и при расхождении прав он.

---

## 0. Как читать этот документ

- **Источник истины: MMBN6 — фундамент боя, MMBN3 — источник системной глубины** [решение 2026-09-18]. Правила боя (поле, движение, урон, неуязвимость, выбор чипов) — как в MMBN6. Контент — чипы, их урон и коды, вирусы и их HP/урон, состав папок — по MMBN3. Если в BN3 чего-то нет, берём из BN6 и помечаем. MMBN1 больше не ориентир; метки `[MMBN1]`/`[MMBN2]` в истории изменений означают значения, которые действовали до 2026-09-18.
- Все числовые значения — **настраиваемые параметры** (см. §17 «Таблица параметров»). В коде нет «магических чисел»: всё берётся из одного конфига и меняется в отладочной панели прямо во время игры.
- Метки у значений:
  - **[MMBN3]** — значение из MMBN3 (таблицы чипов и вирусов, MMKB);
  - **[MMBN6]** — правило боя как в MMBN6 (ссылка на правило, не на конкретные числа);
  - **[оценка]** — точных данных (frame data) не нашли, это стартовое значение для плейтеста;
  - **[решение]** — осознанное отличие от MMBN, принятое для MVP.
- Приоритеты: **P0** — без этого прототипа нет; **P1** — нужно для полного MVP; **P2** — не делать до оценки боя.

### 0.1. Рабочие названия (переименование)

Оригинальные имена — торговые марки Capcom. В документе они оставлены для сверки с MMBN. **В коде и интерфейсе сразу используем рабочие названия**; финальные имена выберем позже.

| Оригинал MMBN | Рабочее название (UI, англ.) | Идентификатор в коде |
|---|---|---|
| MegaMan.EXE (игрок) | Glorp | `player` |
| Mettaur (Mettool) | Mettik | `mettik` |
| Canodumb | Canodron | `canodron` |
| Spikey | Spiker | `spiker` |
| Bunny | Hopzap | `hopzap` |
| Swordy | Bladdy | `bladdy` |
| Ratty | Rattik | `rattik` |
| HardHead | Helmhead | `helmhead` |
| Fishy | Finnik | `finnik` |
| StoneMan (Navi) | Monolith | `monolith` |
| Battle Chip | Chip | `chip` |
| Custom Screen / Custom Gauge | Custom Screen / Custom Gauge | `custom`, `gauge` |
| Folder | Folder | `folder` |
| «Battle Start!» | «BATTLE START!» | — |
| «Enemy Deleted!» | «ENEMY DELETED!» | — |

Названия чипов (Cannon, Sword и т. п.) — обычные слова, в игре пишем их по-английски; полный список — §6.2.

### 0.2. Язык игры

- **Весь текст в игре — на английском**: названия, кнопки, баннеры, подсказки, отладочная панель. Документ остаётся на русском.
- Локализацию (в том числе русскую) добавим позже, P2. Чтобы потом не переписывать интерфейс, **строки не пишем прямо в коде**: все они лежат в таблице `src/i18n/en.ts`, а интерфейс получает их через `t('key')`, например `t('banner.gameOver')` → «GAME OVER».
- Шрифт должен поддерживать кириллицу (задел под локализацию).

**Основные строки интерфейса:**

| Ключ | Текст |
|---|---|
| `title.basic` / `title.field` / `title.random` | Basic folder / Field folder / Random folder |
| `path.title` | STEP {n}/{total} |
| `path.normal` / `path.elite` / `path.boss` | Battle x{n} / Elite x{n} / Boss |
| `path.healed` / `path.fight` | HP restored / Fight |
| `banner.gameOver` | GAME OVER |
| `banner.paused` | PAUSED |
| `banner.runClear` | TERMINAL CLEAR! |
| `btn.resume` / `btn.abandon` / `btn.title` | Resume / Abandon run / Title |
| `btn.debug` | DBG |
| `hud.noChip` | NO CHIP (сегментный дисплей под рядом чипов) |
| `result.step` / `result.total` / `result.hits` | Step / Total time / Hits taken |

---

## 1. Цель MVP

Проверить **боевой цикл MMBN** (правила — MMBN6, контент — MMBN3), прежде чем добавлять свои механики, прогрессию и финальный визуальный стиль.

**Игровая единица:** линейный забег из 10 боёв (§10) [решение 2026-09-18]. Игрок выбирает на титуле одну из трёх стартовых папок, сражается в реальном времени — выбирая чипы не отрываясь от боя — и проходит бои подряд до босса. Смерть заканчивает забег.

**Главный критерий успеха (субъективный):** пройдя бой, разработчик сам хочет начать следующий, потому что решения и исполнение в бою увлекают.

**Платформа:** в первую очередь Google Chrome на Android (портретная ориентация), затем Chrome на десктопе.
**Производительность:** не ниже 60 FPS, в идеале 120 FPS на дисплеях 120 Гц.
**Стек:** Vite + TypeScript + Three.js; интерфейс на HTML/CSS поверх canvas.
**Графика:** только заглушки (§14).
**Звук:** нужен, но не приоритетен (P2); архитектура должна позволять добавить его без переделок (§15.6).

---

## 2. Поле боя

### 2.1. Логическая сетка

В MMBN поле 6×3: игрок слева, враги справа. У нас оно **повёрнуто на 90°**: 3 столбца × 6 рядов, игрок внизу, враги вверху. Правила при этом не меняются.

```
         x=0  x=1  x=2
 y=0   [    ][    ][    ]   ┐
 y=1   [    ][    ][    ]   │ территория ВРАГА (синие панели)
 y=2   [    ][    ][    ]   ┘
 y=3   [    ][    ][    ]   ┐
 y=4   [    ][    ][    ]   │ территория ИГРОКА (красные панели)
 y=5   [    ][    ][    ]   ┘
```

- Координаты: `x ∈ 0..2` (слева направо), `y ∈ 0..5` (сверху вниз).
- Территория врага: `y ∈ 0..2`. Территория игрока: `y ∈ 3..5`.
- **Линия (lane)** — столбец `x`. Термин «ряд» из MMBN у нас соответствует **столбцу x**.
- **«Вперёд» для игрока** — уменьшение `y` (вверх). **«Вперёд» для врага** — увеличение `y` (вниз).
- Игрок не может зайти на территорию врага, враги — на территорию игрока.
- В клетке может стоять только один персонаж (игрок или враг).
- Снаряды, волны и эффекты **не блокируют** клетки и не мешают передвижению.
- **Панели [решение 2026-09-17]** (заменяет «типов панелей нет»). У каждой клетки есть состояние (`NORMAL`, `CRACKED`, `BROKEN`) и владелец. Треснувшая панель становится дырой, когда с неё уходят; на дыру нельзя встать, волны на ней обрываются; дыра восстанавливается через `PANEL_RESTORE_TIME`. Захваченная клетка возвращается владельцу через `STEAL_RESTORE_TIME`, когда освободится (правило MMBN3). Объекты (камень, `ROCK_HP`) занимают клетку и останавливают выстрелы, волны и огненные шары. Подробно — [спека roguelite §3](superpowers/specs/2026-09-17-roguelite-content-design.md).

### 2.2. Отображение

- Панели — плоские прямоугольники с небольшим зазором. Цвета как в MMBN: у игрока красные, у врага синие.
- Камера ортографическая, смотрит сверху под небольшим углом (наклон ~45° от вертикали, `CAMERA_TILT_DEG`, подобрать на глаз). **Всё поле 3×6 всегда в кадре**, на любом соотношении сторон в портрете.
- Поле занимает верхние ~60% экрана, нижние ~40% отданы под управление (§12).

---

## 3. Игрок

| Параметр | Значение | Метка |
|---|---|---|
| Макс. HP | 100 | [MMBN3] стартовое HP |
| Стартовая клетка | (1, 4) — центр своей территории | [MMBN6] |
| Направление взгляда | **всегда вверх (−y)**, не меняется при движении | [MMBN6] |
| Размер | одна клетка | |

### 3.1. Передвижение

- Шаг на одну соседнюю клетку в 4 направлениях, диагоналей нет.
- Шаг не выполняется, если целевая клетка за пределами территории игрока или занята.
- **Логическая позиция меняется сразу при начале шага**, а следующий шаг и визуальная интерполяция используют единый `CELL_MOVE_TIME` = 0,2 с [решение 2026-09-23]. Ввод во время шага буферизуется (одно последнее направление).
- Во время анимации чипа и между ручными атаками committed-заряда двигаться можно. Двигаться нельзя во время оглушения (flinch), паралича и вне состояния ACTION.
- Во время неуязвимости двигаться можно.

### 3.2. Получение урона (§9)

При попадании: вычитается HP → оглушение (flinch) на `PLAYER_FLINCH_TIME` → неуязвимость на `PLAYER_IFRAMES`, спрайт мигает белым [MMBN6: неуязвимый персонаж мигает, атаки не наносят урона].

---

## 4. Buster

Buster удалён [решение 2026-09-18]. Урон наносят только чипы; бой не застревает, потому что использованные чипы возвращаются в очередь добора (§5).

---

## 5. Цикл руки и общий cooldown

Рука содержит пять постоянных слотов. Невыбранные чипы сохраняются, а потраченные слоты пополняются вместе после завершения текущего заряда и общего cooldown [решение 2026-09-22]. Бой при этом не останавливается.

| Параметр | Значение | Метка |
|---|---|---|
| Размер руки `HAND_SIZE` | 5 | [решение 2026-09-18] |
| Общий cooldown руки `HAND_REFILL_COOLDOWN` | 4 с | [оценка 2026-09-22] |
| Видно чипов очереди добора `DRAW_PREVIEW` | 3 | [оценка] |

- Cooldown запускается один раз — при первой атаке заряда — и продолжает идти параллельно ручному применению остальных чипов.
- Если cooldown закончился раньше заряда, пополнение становится `ready`, но остаётся утопленным и недоступным до завершения или прерывания заряда.
- Если заряд закончился раньше cooldown, вся оставшаяся рука заблокирована до конца таймера. Новый заряд собирать нельзя.
- Когда выполнены оба условия, все действительно потраченные слоты пополняются одновременно. Невыбранные, возвращённые и намеренно пустые слоты не меняются.
- Прерывание заряда уроном не сбрасывает и не перезапускает cooldown.
- Если очередь добора пуста, использованные чипы перемешиваются и становятся новой очередью добора (событие `drawReshuffled`) [решение 2026-09-18]. Готовый слот остаётся пустым, только если перемешивать нечего.

---

## 6. Чипы и папка

### 6.1. Модель данных [решение 2026-09-17]

Чип — это данные: куда бьёт (`shape`), что делает попадание (`onHit`), что делает с полем (`field`), лечение и невидимость. Подробно — [спека roguelite §4](superpowers/specs/2026-09-17-roguelite-content-design.md), код — `src/data/chips.ts`.

```ts
type ChipCode = 'A'|'B'|...|'Z'|'*';
interface ChipDef {
  id: ChipId;
  power: number | null;          // урон; null — без урона
  kind: 'attack'|'support'|'field';  // support и field срабатывают сразу
  useTime: 'VULCAN'|'CANNON'|'SWORD'|'BOMB'|'RECOVER'|'FIELD';  // профиль Startup/Recovery
  codes: ChipCode[];             // возможные коды (случайная папка выбирает один)
  rarity: 'common'|'uncommon'|'rare';  // веса случайной папки (§6.3)
  shape: Shape;                  // §6.4
  onHit?: { push?: true; paralyze?: true };
  field?: 'crackAll'|'breakEnemy'|'areaGrab'|'grabPanel'|'breakAhead'|'breakRowAhead'|'repair'|'rock';
  heal?: number;
  invis?: true;
}
interface ChipInstance {         // конкретная карта в папке
  uid: number;
  defId: ChipId;
  code: ChipCode;
  state: 'folder'|'hand'|'pending'|'queued'|'used';
  deal: number;                  // номер раздачи; у повторно розданного чипа новый (§7.5)
}
```

Имена и описания — в `i18n` (`chip.<id>.name` / `.desc`). **Сравнение «одинаковое имя» идёт по `defId`**, а не по строке имени.

### 6.2. Набор чипов [MMBN3]

Урон и коды — MMBN3 (MMKB, «List of Mega Man Battle Network 3 Battle Chips») [решение 2026-09-18]. Чипы, которых нет в BN3 (LilBomb, CrosBomb, Quake1, Crack), удалены. Время использования и длительности эффектов — [оценка] (§17). Имена в UI — как в BN3, не длиннее 8–9 символов.

| Чип (UI) | id | Урон | Коды | Форма и эффект | Редкость |
|---|---|---|---|---|---|
| Cannon | `cannon` | 40 | A B C D E * | `lane` | common |
| HiCannon | `hicannon` | 60 | H I J K L * | `lane` | uncommon |
| M-Cannon | `mcannon` | 80 | O P Q R S | `lane` | rare |
| AirShot | `airshot` | 20 | * | `lane`, отталкивание | common |
| ShotGun | `shotgun` | 30 | B F J N T * | `lane` + клетка за целью | common |
| V-Gun | `vgun` | 30 | D G L P V * | `lane` + 2 клетки за целью по диагонали | common |
| SideGun | `sidegun` | 30 | C H M S Y * | `lane` + клетки слева и справа от цели | common |
| Spreader | `spreader` | 30 | M N O P Q * | `lane` + 3×3 вокруг цели | uncommon |
| Sword | `sword` | 80 | E H L S Y | `near` 1 клетка | common |
| WideSwrd | `widesword` | 80 | C E L Q Y | `near` ряд из 3 | uncommon |
| LongSwrd | `longsword` | 80 | E I L R Y | `near` 2 клетки | uncommon |
| MiniBomb | `minibomb` | 50 | B G L O S * | `lob` 3, одна клетка | common |
| ShockWav | `shockwave` | 60 | D H J L R | `wave` | common |
| ZapRing1 | `zapring` | 20 | A M P Q S * | `lane`, паралич `PARALYZE_TIME` | common |
| Recov10 | `recov10` | +10 HP | A C E G L * | `self` | common |
| Recov30 | `recov30` | +30 HP | B D F H M * | `self` | common |
| Recov50 | `recover50` | +50 HP | C E G I N * | `self` | uncommon |
| Recov80 | `recov80` | +80 HP | D F H J O * | `self` | rare |
| Invis | `invis` | — | B E F R S * | `self`, невидимость `INVIS_TIME` | uncommon |
| Geddon1 | `geddon1` | — | D J M O S * | трещины на всех свободных клетках поля, свои тоже | uncommon |
| Geddon2 | `geddon2` | — | F H N O W | дыры на свободных клетках врага (в BN3 — на всём поле) [решение 2026-09-17] | rare |
| AreaGrab | `areagrab` | — | E L R S Y * | захват свободных клеток ближнего вражеского ряда | uncommon |
| PanlGrab | `panlgrab` | — | A H L S Y * | захват ближней вражеской клетки в своей колонке | common |
| PanlOut1 | `panlout1` | — | A B D L S * | дыра на клетке впереди | common |
| PanlOut3 | `panlout3` | — | C E N R Y | дыры на ряду из 3 клеток впереди | uncommon |
| Repair | `repair` | — | A C D F S * | все свои клетки → `NORMAL` | common |
| RockCube | `rockcube` | — | A C E H R * | камень на свою свободную клетку впереди | common |

Заметки:

- Всего 27 чипов. Barrier, Vulcan, Guard и другие новые механики отложены [решение 2026-09-18].
- Steal переименован в AreaGrab (`areagrab`), как в BN3.
- Код `*` подходит к любому коду.

### 6.3. Стартовые папки [решение 2026-09-18]

Три стартовые папки по 30 чипов (`FOLDER_SIZE`); игрок выбирает одну на титуле (§10). Папка за забег не меняется: наград нет. Состав — `src/data/folders.ts`, случайная — `src/app/randomFolder.ts`.

| Папка | Суть |
|---|---|
| **BASIC** (`basic`) | Пушки, мечи и бомбы, в основном на кодах B / L / S; два чипа поля (PanlOut1, AreaGrab); лечение Recov10 ×2, Recov30 ×2 |
| **FIELD** (`field`) | Средний уровень, построена вокруг панелей: HiCannon, Spreader, AreaGrab ×2, PanlGrab, PanlOut1, PanlOut3, Geddon1, RockCube, Repair, Invis; лечение Recov30 ×2 |
| **RANDOM** | Собирается сидом забега по правилам ниже |

Правила случайной папки [оценка]:

- 30 чипов; чип выбирается по весам редкости common 6 / uncommon 3 / rare 1;
- не больше 4 копий одного чипа (`RANDOM_MAX_COPIES`);
- не меньше 3 лечащих чипов (`RANDOM_MIN_HEALS`);
- не больше 4 чипов поля (`RANDOM_MAX_FIELD`);
- забег выбирает 3 «основных» кода; чип, у которого есть основной код или `*`, берёт его с шансом 70%, иначе — случайный код из своих.

**Отладочная папка `all`** (`?folder=all`, DBG → Session → folder) — по одному экземпляру каждого чипа с первым кодом. Отладочные переходы (`?battle=`, `?encounter=`) берут папку из `?folder=` (по умолчанию `basic`).

### 6.4. Формы атак

Игрок стоит в клетке `(px, py)`; «вперёд» означает `−y`. Смещения заданы относительно игрока или цели; клетки за пределами поля отбрасываются.

| Форма | Какие клетки поражает | Особенности |
|---|---|---|
| `lane` | Первая цель (враг или объект) в `(px, py−1)…(px, 0)`, плюс клетки `around` вокруг неё | Мгновенно после Startup |
| `near` | Клетки `cells` относительно игрока | Мечи: Sword `(0,−1)`, WideSwrd ряд перед игроком, LongSwrd 2 клетки [MMBN3] |
| `lob` | Клетка `(px, py−depth)` и область `area` вокруг неё | Время полёта = пройденные клетки × `CELL_TRAVEL_TIME`; урон при приземлении. MiniBomb падает на 3 клетки вперёд [MMBN3] |
| `wave` | Колонка игрока вперёд, начиная с `(px, py−1)` | Волна идёт по клетке за `FAST_CELL_TRAVEL_TIME`, бьёт каждого врага один раз, обрывается на дыре, краю поля и объекте |
| `self` | игрок | Лечение `heal`, невидимость `INVIS_TIME` |

Эффекты попадания (`onHit`): `push` — враг отходит на ряд назад, если клетка своя и свободна; `paralyze` — враг стоит `PARALYZE_TIME`, его таймеры тоже стоят.

Действия с полем (`field`, `World.applyFieldAction`):

| Действие | Чип | Что делает |
|---|---|---|
| `crackAll` | Geddon1 | трещины на всех свободных клетках поля |
| `breakEnemy` | Geddon2 | дыры на свободных вражеских клетках |
| `areaGrab` | AreaGrab | свободные клетки ближнего вражеского ряда становятся своими на `STEAL_RESTORE_TIME` |
| `grabPanel` [решение 2026-09-18] | PanlGrab | ближняя вражеская клетка в колонке игрока становится своей, если свободна |
| `breakAhead` [решение 2026-09-18] | PanlOut1 | дыра на клетке перед игроком |
| `breakRowAhead` [решение 2026-09-18] | PanlOut3 | дыры на всём ряду из 3 клеток перед игроком |
| `repair` | Repair | все свои клетки → `NORMAL` |
| `rock` | RockCube | камень на клетку впереди, если она своя и свободна |

Дыры PanlOut1/PanlOut3 бьют и по своим клеткам, как в BN3: своя клетка впереди тоже становится дырой. Под персонажем или объектом `breakPanel` даёт только трещину.

Важные следствия, как в MMBN:

- **Мечи бьют только соседнюю клетку.** Меч достаёт до врага, только если игрок стоит в переднем ряду (`py = 3`), а враг — в `y = 2`. LongSword из `py = 3` достаёт до `y = 2` и `y = 1`. Из заднего ряда игрока меч бьёт по пустой клетке своей территории.
- MiniBomb из `py = 3` падает в `y = 0`, из `py = 4` — в `y = 1`, из `py = 5` — в `y = 2`.
- Одна атака наносит урон каждой цели **не больше одного раза** (реестр попаданий у каждой атаки).

### 6.5. Использование чипа

- Первое успешное нажатие Attack фиксирует выбранный заряд и запускает только его первый чип. Каждый следующий чип требует отдельного нажатия Attack [решение 2026-09-22].
- Каждое действие проходит `Startup → Impact/Active → Recovery` [решение 2026-09-23]. Быстрые чипы имеют Startup 0,1 с и Recovery 0,1–0,15 с; более commit-heavy — Startup 0,15–0,2 с и Recovery 0,15–0,2 с. Движение не блокируется, но следующий чип ждёт конца Recovery.
- Hitscan, мечи и support срабатывают после Startup; Vulcan сохраняет интервалы между активными попаданиями; бомба наносит урон при приземлении.
- В момент запуска каждого чипа фиксируется текущая клетка игрока. Все его попадания, включая очередь Vulcan, используют эту клетку; следующий ручной запуск фиксирует уже новую позицию.
- Одно нажатие Attack за `ACTION_BUFFER_TIME` = 0,1 с до конца action lock сохраняется и выполняется сразу после Recovery. Несколько inputs не накапливаются; flinch, paralysis, пауза и конец боя очищают буфер [решение 2026-09-23].
- Если игрок получает удар до `hitTick`, текущий чип возвращается в исходный слот. После `hitTick` эффект и расход сохраняются. В обоих случаях весь ещё не запущенный хвост возвращается в руку, мигает красным и заряд завершается; cooldown продолжает идти.
- Использованный чип получает `used` и покидает слот. Замена резервируется после `hitTick`, но активируется только по правилам §5.
- Когда выбранного или committed-заряда нет, Attack «глухой».

---

## 7. Рука и заряд

Кастом-экран удалён целиком [решение 2026-09-18]. Выбор чипов идёт в реальном времени, прямо в бою, без паузы и без отдельного экрана. Кнопка ADD и рука из 10–15 чипов удалены вместе с ним.

### 7.1. Состояния чипа в руке

| Состояние | Когда | Нажатие |
|---|---|---|
| `ready` | доступен и совместим | добавляет в редактируемый заряд |
| `queued` | выбран до первой атаки, с номером порядка | повторный тап снимает |
| `blocked` | несовместим с редактируемым зарядом (§7.3) | не реагирует |
| `committed` | остаётся в зафиксированном хвосте | не реагирует; запускается только Attack |
| `locked` | не входит в committed-заряд или рука ждёт cooldown; показывает общий прогресс cooldown | не реагирует |
| `cooling` / `empty` | слот потрачен; замена уже видна / ещё не зарезервирована | не реагирует |

### 7.2. Формирование заряда

- Тап по `ready`-чипу добавляет его **в конец** заряда. Порядок тапов = порядок ручных атак, номер виден на кассете.
- Первый добавленный чип задаёт правило совместимости. Правило держится до запуска или очистки выбранного заряда.
- В заряд входит не больше `HAND_SIZE` чипов.
- Повторный тап по `queued`-чипу снимает его, заряд уплотняется. Так можно менять состав и порядок до первой атаки.
- Первая атака переводит заряд в `committed`: состав и порядок хвоста фиксированы до завершения или прерывания.
- После активации следующей руки правило сбрасывается: новый заряд можно начать с любого `ready`-чипа.

### 7.3. Правило совместимости [MMBN6]

Набор `S` допустим, если выполнено одно из условий:

- у всех чипов `S` одинаковый `defId`, **или**
- существует код `K`, такой что код каждого чипа в `S` равен `K` или `*`.

Правила не смешиваются: Cannon A + Cannon F — допустимо (одно имя); Cannon A + Recover50 A — допустимо (один код); Cannon A + Cannon F + Recover50 A — недопустимо.

Пересчёт после каждого изменения заряда. Несовместимые чипы получают `blocked` **в том же кадре**: игрок узнаёт совместимость по свечению, а не вычисляет её под огнём.

### 7.4. Атака

- Каждое нажатие Attack запускает ровно один чип: первый — из редактируемого заряда, последующие — из committed-хвоста.
- Каждый запущенный чип получает `used` и физически вылетает; момент следующего запуска полностью контролирует игрок.
- Выбор во время committed-фазы и ожидания cooldown заблокирован.
- Пустой заряд или занятый игрок — «глухое» нажатие: команда не отправляется.

### 7.5. Очередь добора (Draw Queue)

- Папка перемешивается один раз в начале боя детерминированным ГСЧ с сидом (§15.4). Что осталось после раздачи руки — очередь добора.
- Игрок видит ближайшие `DRAW_PREVIEW` чипов. Показ приглушённый и мельче руки: это подсказка для планирования, а не орган — нажимать там нечего.
- После необратимого расхода чипа следующий чип очереди резервируется для его слота и исчезает из preview. Все зарезервированные замены активируются вместе (§5).
- Когда очередь пуста, использованные чипы перемешиваются тем же ГСЧ и становятся новой очередью (событие `drawReshuffled`) [решение 2026-09-18]. Так длинный бой не остаётся без чипов. Каждая раздача даёт чипу новый номер `deal`: чип, который сработал и тут же вернулся в тот же слот, терминал всё равно выбрасывает и загружает заново [решение 2026-09-18].

### 7.6. Начало боя

- Рука раздаётся сразу: пять чипов уже в гнёздах, когда бой начинается.
- `BATTLE_INTRO` сохраняется (0,8 с): враги появляются, кассеты въезжают, игрок неуязвим. Это единственный момент боя, когда симуляция стоит.
- Состояние `CUSTOM` и баннер `BATTLE START!` удалены.

---

## 8. Враги

### 8.1. Общие правила

- Враги живут на своей территории (`y ∈ 0..2`), в клетке может быть только один персонаж.
- Полная атака использует единую грамматику `INTENTION → LOCK → COUNTER → STRIKE → RECOVERY` [решение 2026-09-23]. `INTENTION` сообщает тип действия, `LOCK` фиксирует направление/клетки, `COUNTER` даёт короткое окно, `STRIKE` ровно один раз создаёт угрозу, `RECOVERY` отделяет следующую атаку.
- Counter определяется состоянием врага **в тик фактического попадания**, а не нажатием Attack. Только урон в `COUNTER` отменяет ещё не выпущенную атаку и переводит врага в `STAGGER`, затем в `IDLE`.
- Попадание вне Counter Window не прерывает действие — это только визуальная вспышка (`HIT_FLASH` 0,1 с) и цифра урона. У врагов нет неуязвимости после удара.
- Counter не останавливает серию игрока. Получение игроком реального урона, наоборот, отменяет активное звено и весь оставшийся хвост (§6.5). Barrier поглощает удар и не отменяет серию.
- `HP ≤ 0` → состояние `DEAD`: анимация удаления 0,4 с, затем враг убирается, клетка освобождается. Атаки, которые уже летят (волны, снаряды), **продолжают лететь** [MMBN6].
- Атаки врага — отдельные сущности (§8.5), их можно увидеть и от них можно уклониться.
- Визуальный язык врагов различается, но порядок фаз общий. Зафиксированные клетки подсвечиваются в `LOCK/COUNTER`; debug overlay показывает фазу и оставшееся время.
- HP и урон вирусов уровня 1 — MMBN3 [решение 2026-09-18]; тайминги — [оценка].

### 8.2. Mettik (Mettaur) — тайминг, волна по линии [MMBN3]

| Параметр | Значение | Метка |
|---|---|---|
| HP | 40 | [MMBN3] |
| Урон волны | 10 | [MMBN3] |
| Профиль фаз | standard: 0,3 / 0,2 / 0,18 / 0,1 / 0,3 с | [решение 2026-09-23] |
| Интервал шага `MOVE_TIME` | 0,2 с | [решение 2026-09-23] |
| Скорость волны | `CELL_TRAVEL_TIME` = 0,2 с/клетка | [решение 2026-09-23] |

Поведение (в MMBN: «двигается вверх-вниз по своему столбцу, атакует, когда стоит на одном ряду с игроком»; у нас это перемещение по x):

1. `IDLE/MOVE`: Mettik остаётся в своём ряду `y` и каждые `MOVE_TIME` делает шаг по `x` к линии игрока.
2. При совпадении линии и наличии права атаки проходит пять общих фаз; линия фиксируется и подсвечивается в `LOCK`.
3. В `STRIKE` создаёт `Shockwave` в `(x, y+1)`. Волна идёт вниз по одной клетке за `CELL_TRAVEL_TIME`, проходит насквозь и бьёт каждую цель один раз.
4. `RECOVERY` → `IDLE`.
5. Если игрок ушёл из линии во время телеграфа, атака **всё равно выполняется** по исходной линии.

**Очерёдность** [MMBN3]: если в бою несколько Mettik, атакует только один. Остальные «прячутся в каски» и не атакуют, но двигаются. Право атаки переходит по кругу после `RECOVERY` атаковавшего. Реализация: общий `MettikTurnToken`.

### 8.3. Canodron (Canodumb) — давление по линии [MMBN3]

| Параметр | Значение | Метка |
|---|---|---|
| HP | 60 | [MMBN3] (было 50) |
| Урон выстрела | 10 | [MMBN3] |
| Передвижение | нет, стоит на месте | [MMBN3] |
| Скорость прицела `CANO_CURSOR_STEP` | 0,15 с на клетку | [оценка] |
| Профиль фаз | standard: 0,3 / 0,2 / 0,18 / 0,1 / 0,3 с | [решение 2026-09-23] |

Поведение [MMBN: «если стоишь перед ним, он пускает прицел по ряду; если стоишь на панели с прицелом, стреляет по ряду»]:

1. `IDLE`: ждёт, пока игрок окажется в его линии (`player.x == x`).
2. `INTENTION` (прицел): в клетке `(x, y+1)` появляется курсор, который каждые `CANO_CURSOR_STEP` сдвигается вниз и удерживает найденную цель до минимального времени Intention.
   - Если игрок ушёл из линии — прицел исчезает, возврат в `IDLE` (без перезарядки).
   - Если прицел дошёл до `y = 5` и не встретил игрока — прицел исчезает, возврат в `IDLE`.
   - Если прицел оказался с игроком в одной клетке — начинается `LOCK`, затем `COUNTER`.
3. `STRIKE`: мгновенный выстрел по зафиксированной линии `x`, попадает в первую цель в `y+1…5`. Если игрок успел уйти — промах.
4. `RECOVERY` → `IDLE`.

### 8.4. Spiker (Spikey) — мобильное давление [MMBN3]

| Параметр | Значение | Метка |
|---|---|---|
| HP | 90 | [MMBN3] |
| Урон HeatShot | 30 | [MMBN3] (стихия Огонь игнорируется — стихий в MVP нет) |
| Кол-во телепортов до атаки | 2–4 случайно | [оценка] |
| Профиль фаз и движения | fast: 0,2 / 0,15 / 0,15 / 0,1 / 0,25; move 0,15 с | [решение 2026-09-23] |
| Скорость огненного шара | `FAST_CELL_TRAVEL_TIME` = 0,15 с/клетка | [решение 2026-09-23] |

Поведение [по описаниям MMBN2–3: «телепортируется случайным образом, ненадолго останавливается на ряду игрока, целится и стреляет»; HeatShot поражает цель и клетку за ней]:

1. `MOVE`: каждые `MOVE_TIME` телепортируется в случайную свободную клетку своей территории.
2. После N телепортов телепортируется в свободную клетку **в линии игрока** (`x = player.x`, случайный `y`). Если такой клетки нет — ещё один случайный телепорт, и попытка повторяется.
3. Проходит общие `INTENTION → LOCK → COUNTER`; линия подсвечивается после фиксации.
4. В `STRIKE` огненный шар летит вниз от `(x, y+1)` по клетке за `FAST_CELL_TRAVEL_TIME`. При попадании взрыв задевает клетку за целью; промахнувшийся шар улетает за край.
5. `RECOVERY` → `MOVE`.

### 8.5. Уровни, щит, паралич [решение 2026-09-17]

- У врага есть уровень 1–3 (`ENEMY_LEVELS` в `data/enemies.ts`): множители HP, урона и скорости действий (тайминги делятся на скорость). Уровень 2 — HP ×1,5, урон ×1,5, скорость ×1,1; уровень 3 — ×2 / ×2 / ×1,2 [решение 2026-09-18]. Варианты уровня 2 из BN3 (Mettaur2 60/40 и т. п.) рассчитаны на выросший HP игрока, а у нас HP 100. Над HP-сегментами врага уровня ≥ 2 горят точки уровня.
- `guarded`: удар не проходит, клетка вспыхивает (шлем Helmhead, рывок Finnik).
- Паралич игрока (`paralyzeTicks`): нельзя ходить и использовать чипы; неуязвимости не даёт.

### 8.6. Новые вирусы [MMBN3]

HP и урон — MMBN3 (уровень 1) [решение 2026-09-18], тайминги — [оценка], группы `hopzap`, `bladdy`, `rattik`, `helmhead`, `finnik` в tuning. Подробно — [спека roguelite §5.2](superpowers/specs/2026-09-17-roguelite-content-design.md).

| Враг | HP / урон | Поведение |
|---|---|---|
| Hopzap (Bunny) | 40 / 15 | Прыгает по своим клеткам в колонку игрока → телеграф → медленное кольцо вниз; попадание парализует `HOP_PARALYZE`. |
| Bladdy (Swordy) | 90 / 30 | Идёт к переднему краю своей зоны в колонке игрока → телеграф → удар по двум клеткам перед собой. |
| Rattik (Ratty) | 40 / 20 | Держит ряд, шагает к колонке игрока → мина вниз по колонке; на ряду игрока мина один раз сворачивает к нему. Дыра и объект её останавливают. |
| Helmhead (HardHead) | 80 / 60 | В шлеме неуязвим `HELM_CLOSED`; открывается, отмечает клетку игрока, бросает ядро: урон и дыра в панели (под игроком — трещина). |
| Finnik (Fishy) | 90 / 30 | Телеграф колонки → рывок насквозь; во время рывка не на поле и неуязвим; возвращается на свою клетку или ближайшую свободную. |

Punchy (Champy) удалён [решение 2026-09-18]: его нет в BN3. Вместе с ним удалено отталкивание игрока — им пользовался только Punchy. Позже добавим другой вирус.

### 8.7. Босс Monolith (StoneMan) [решение 2026-09-17]

HP 400 [решение 2026-09-18] (StoneMan нет в BN3; было 600), без свиты, спрайт 48×48 и шире обычного. Бродит по своей зоне; раз в `MONO_ATTACK_INTERVAL` выбирает атаку: камнепад на клетку игрока и ещё 2 (в гневе — 4) случайные клетки его зоны (урон и трещины), каменный куб в зоне игрока (вражеский объект), волна по колонке игрока, трескающая клетки. Ниже 50% HP атакует в `MONO_RAGE_SPEED` раз чаще. Подробно — [спека roguelite §5.3](superpowers/specs/2026-09-17-roguelite-content-design.md).

### 8.8. Атаки-сущности врагов

| Сущность | Движение | Коллизия | После попадания |
|---|---|---|---|
| `Shockwave` (Mettik) | по клеткам вниз, `CELL_TRAVEL_TIME` на клетку | с игроком в текущей клетке | продолжает идти |
| `CannonShot` (Canodron) | мгновенно | первая цель в линии | исчезает |
| `HeatShot` (Spiker) | по клеткам вниз, `FAST_CELL_TRAVEL_TIME` на клетку | с игроком или объектом в текущей клетке | взрыв + клетка за целью, исчезает |
| `LaneShot` zapring / dash (Hopzap, Finnik) | по клеткам вниз | с объектом или игроком | исчезает (кольцо парализует) |
| `RatMine` (Rattik) | вниз, затем один поворот вбок | с объектом или игроком; обрывается на дыре | исчезает |
| `CannonBall` / rockfall (Helmhead, Monolith) | по дуге; время = пройденные клетки × `CELL_TRAVEL_TIME` | клетки приземления | урон, трещина или дыра |

- Коллизия проверяется **каждый тик**: клетка атаки == клетка игрока, игрок не неуязвим, атака ещё не била этого игрока → урон.
- Позиция снаряда логически дискретна (клетка), визуально интерполируется между клетками.
- Базовая дизайнерская единица `Combat Timing Unit` = 0,1 с; она не является simulation timestep [решение 2026-09-23].

---

## 9. Урон, оглушение, неуязвимость

- Урон — целое вычитание HP, `hp = max(0, hp − dmg)`. Защиты, брони, стихий и статусов нет.
- **Игрок после попадания:**
  - оглушение (flinch) `PLAYER_FLINCH_TIME` = 0,4 с [оценка]: нельзя двигаться и использовать чипы; анимация чипа прерывается;
  - неуязвимость `PLAYER_IFRAMES` = **2,0 с** с момента попадания [оценка по серии; в v1 было 250 мс — слишком мало, враги смогут бить почти подряд]. Спрайт мигает белым. Атаки, попавшие в неуязвимого игрока, урона не наносят и не считаются попаданием.
- Одна атака бьёт одну цель не больше одного раза (реестр `hitTargets` у атаки). Урон «каждый кадр» исключён.
- Цифры урона всплывают над целью (0,6 с).

---

## 10. Забег [решение 2026-09-18]

Линейный забег из 10 боёв; заменяет путь с выбором, награды и наследие из [спеки roguelite §6](superpowers/specs/2026-09-17-roguelite-content-design.md). Код — `src/app/run.ts`, `src/app/randomFolder.ts`, `src/app/session.ts`.

### 10.1. Старт

- Титул предлагает три старта: **Basic folder**, **Field folder**, **Random folder** (§6.3). Выбор задаёт папку на весь забег.
- Сид забега выводится из сида сессии; из него — случайная папка, встречи и сиды боёв.

### 10.2. Путь

- 10 шагов подряд, выбора нет (`RUN_STEPS`). Шаги 5 и 8 — элитные бои (`ELITE_STEPS`) [оценка], шаг 10 — босс Monolith, остальные — обычные бои.
- Встреча шага выбирается сидом из `data/encounters.ts` по типу и глубине, без повторов в одном забеге (14 обычных, 4 элитных, босс).
- Экран PATH — меню на CRT: `STEP 04/10`, тип и число врагов следующего боя (`Battle x2`, `Elite x2`, `Boss`), HP, размер папки, пункт `Fight`.

### 10.3. HP и папка

- HP переносится между боями. После побед на шагах 3, 6 и 9 HP восстанавливается полностью (`HEAL_EVERY` = 3); PATH пишет `HP restored` над подсказкой, тип следующего боя остаётся в подзаголовке [решение 2026-09-18]. Кроме этого лечат только чипы Recov.
- Папка за забег не меняется: наград, экрана REWARD и роста папки нет [решение 2026-09-18]. Перед каждым боем она тасуется заново.

### 10.4. Победа и поражение

- Все враги удалены → `BATTLE_WON`: сигнал поля → PATH следующего шага. После босса — экран COMPLETE (`TERMINAL CLEAR!`: число боёв, общее время, попадания, остаток HP) → TITLE.
- Игрок: `HP ≤ 0` → `PLAYER_DEAD` → экран GAME_OVER: шаг, общее время, попадания; пункт `Title` → TITLE.
- Наследия и поколений нет [решение 2026-09-18]; сохранений между забегами нет. RETRY нет. В паузе есть ABANDON — это тоже смерть (→ GAME_OVER).
- Если игрок и последний враг погибли в одном тике, **засчитывается поражение** [решение].

### 10.5. Туториал [решение 2026-09-19]

- Обучение — отдельный режим сессии (`Session.mode = 'tutorial'`, `src/app/tutorial/`), не забег: он не создаёт `Run`, не переносит HP или папку между боями и не пишет в историю результатов. Титул предлагает четвёртый пункт `TUTORIAL` под тремя папками (§10.1).
- Четыре боя подряд без экрана PATH между ними: победа сама заряжает следующий бой (`Session.updateTutorial` реагирует на тот же переход `BATTLE_WON`, что и обычный забег, §10.4). После четвёртого боя — экран `TUTORIAL CLEAR`, единственный пункт которого возвращает на титул.
- Бои прописаны построчно (`src/data/tutorial.ts`, один фиксированный сид на весь туториал), а не собраны из папки: точный стартовый набор чипов и точная рука на каждый бой, без перетасовки.
  1. **Бой 1 — один Меттик, пустая рука.** Учит шагу трекболом (3 шага и одна пережитая волна) и подаче чипа: кассета `Cannon A` сама влетает в средний слот рейла, тап по ней ставит чип в очередь, тап по трекболу стреляет и добивает.
  2. **Бой 2 — два Меттика, рука `Cannon A` / `Cannon A` / `Shotgun B`.** Учит совместимости чипов по имени или коду (§7.3): выбор `Cannon` гасит `Shotgun` в тот же кадр, потому что у них нет ни общего имени, ни общего кода.
  3. **Бой 3 — Меттик на дальнем ряду, рука `PanlGrab L` / `Sword L`.** Учит захвату клетки: меч не достаёт вражеский ряд с территории игрока, `PanlGrab` отбирает ближайшую к игроку клетку врага — встать на неё и рубить.
  4. **Бой 4 — Canodron и Меттик, полная рука, подсказок нет.** Экзамен: использованные слоты сами добирают новые чипы после кулдауна, без напоминаний.
- Лесенка подсказок эскалирует по бездействию текущего такта (`TutorialDirector.hint`, тайминги в §17): на 2 с — усиленное «дыхание» нужного контрола (трекбола или кассеты, смотря чего ждут от игрока), на 4 с — слово на 14-сегментном дисплее вместо `SELECT CHIP`, на 8 с — строка на CRT (TERMINAL.md §6/§8). Любое засчитываемое действие — шаг, чип в очереди, выстрел — сбрасывает таймер такта и снимает подсказку. Такт без текста подсказки (сейчас это только экзамен четвёртого боя, `seg`/`line` = null) не подсказывает вовсе — ни текстом, ни усиленным пульсом контрола [решение 2026-09-19].
- Игрок не может погибнуть: туториал всегда запускается с `cheats.noKo`, которое держит HP не ниже 1 (`World`, §9), так что `PLAYER_DEAD`/`GAME_OVER` в туториале не наступают.
- Пауза в туториале ведёт не в ABANDON-поражение, а сразу на титул: досрочный выход не засчитывается как смерть.

---

## 11. Состояния игры

Экраны сессии (`session.screen`): `TITLE → PATH → BATTLE → PATH … → BATTLE (босс) → COMPLETE → TITLE`; смерть или ABANDON → `GAME_OVER → TITLE`; `PAUSED` — пауза боя.

Состояния боя (`world.state`):

```
BATTLE_INTRO ─→ ACTION
                  │ │
 PAUSED ←─[пауза]─┘ │
                    ↓
   PLAYER_DEAD (→ GAME_OVER)   BATTLE_WON (→ PATH / COMPLETE)
```

Выбор чипов больше не имеет своего состояния: он идёт внутри `ACTION` [решение 2026-09-18].

| Состояние | Симуляция | Описание |
|---|---|---|
| `BOOT` | — | загрузка |
| `TITLE` | — | название, три старта (Basic / Field / Random folder), подсказка по управлению |
| `BATTLE_INTRO` | стоит | поле строится, враги появляются, рука въезжает в гнёзда (0,8 с) |
| `ACTION` | идёт | реальное время, включая выбор чипов и восстановление слотов |
| `PAUSED` | стоит | меню: Resume / Abandon. Включается кнопкой паузы и **автоматически** при `visibilitychange` (вкладка скрыта) |
| `PLAYER_DEAD` | стоит (кроме анимации) | 1,0 с |
| `DEFEAT` | — | не используется: поражение показывает экран сессии `GAME_OVER` |
| `BATTLE_WON` | стоит | баннер «ENEMY DELETED!» 1,2 с |
| `RESULT` | — | не используется: после победы — экран `PATH` |
| `SEQUENCE_COMPLETE` | — | не используется: финал — экран `COMPLETE` |

Переходы делает один конечный автомат (`BattleStateMachine`). Ввод игры обрабатывается **только** в `ACTION`.

---

## 12. Управление

С 2026-09-17 игрок управляет боем через физический терминал NET-01; органы, жесты, мышь и клавиатура подробно описаны в [TERMINAL.md §5, §6, §8](TERMINAL.md) [решение 2026-09-17]. Кратко:

- **Телефон, две руки** [решение 2026-09-18]: правый большой палец на трекболе — рывок даёт ровно один шаг; не отрывая пальца, следующий рывок после остановки (`SWIPE_REARM_TIME`) или смены направления даёт следующий шаг, а длинная протяжка в одну сторону — всё равно один шаг [решение 2026-09-19]; **тап** запускает текущий чип заряда (срабатывает на отпускании, если жест не дал шага; удержание дольше `TAP_MAX_TIME` тапом не считается). Левый большой палец — **тапы по кассетам в ряду**: набор и редактирование заряда до первой атаки (§7.2). Пауза — клавиша в верхнем углу.
- Разделение рук намеренное: тап по кассете уводит палец с трекбола, поэтому набор серии заранее, в спокойный момент, — и есть цена, которую платит игрок за длинную связку.
- **Мышь:** те же органы; перетаскивание по трекболу — шаг, клик по кассете — выбор.
- **Клавиатура:** WASD/стрелки — шаг, удержание клавиши — повтор (`HOLD_REPEAT_DELAY` = 0,35 с, затем каждые `HOLD_REPEAT` = 0,2 с); Space или F — выстрел (без задержки тапа); клавиши 1–5 — выбор чипа в соответствующем слоте; Esc — пауза. В меню: стрелки — курсор, Enter/Space — выбрать.
- Отключаем `touch-action`, выделение текста, контекстное меню, «pull-to-refresh» и двойной тап для зума. Экран не гаснет (Wake Lock API, если доступен).
- **Жесты браузера и мессенджеров:** `touchmove` отменяется, `overscroll-behavior` выключен, жест/кнопка «назад» перехватывается через History API и ставит бой на паузу. Нативный жест закрытия встроенного браузера (например, в Telegram) страница отменить не может — рекомендуется открывать игру в Chrome или добавить на домашний экран (есть манифест, запуск на весь экран).

### 12.1. Слой ввода

Всё управление превращается в **абстрактные команды** (`Move(dir)`, `UseChip`, `SelectChip(slot)`), которые симуляция забирает раз в тик [решение 2026-09-18]. Органы терминала и клавиатура — только источники этих команд. Отдельного пути в обход команд больше нет: выбор чипа идёт в `ACTION`, как и всё остальное, поэтому `world.custom*()` удалены.

---

## 13. Интерфейс (HUD)

HUD, баннеры и меню рисуются **внутри CRT** терминала пиксельным шрифтом — [TERMINAL.md §7–8](TERMINAL.md) [решение 2026-09-17]. HTML остаётся только для кнопки DBG, отладочной панели и оверлея.

**В бою** [решение 2026-09-19]: в CRT — только HP игрока (слева снизу, янтарный при HP ≤ 25%, мигает при уроне), HP над каждым врагом, всплывающие цифры урона и лечения. Будущие чипы показаны под рядом, а дисплей показывает следующий чип заряда. Красное кольцо трекбола горит, когда тап может запустить чип или выбрать пункт меню. Баннеры «BATTLE n/4» и «BATTLE START!» удалены; победа и поражение показываются графическими сигналами сетки.

---

## 14. Визуальный прототип

Визуал поля боя описан в [BATTLE_VISUAL.md](BATTLE_VISUAL.md) («CRT Occult Vector») [решение 2026-09-17]:

- чёрный фон, светящаяся сетка в перспективе, сигнальные цвета палитры; неполная яркость — более тусклый сплошной цвет, без дизеринга [решение 2026-09-19];
- враги — процедурные симметричные пиксельные существа (сид на вид врага, `data/enemies.ts`), игрок — пиксельный спрайт;
- в бою нет текста: HP врагов — сегменты, урон — крупные пиксельные числа, начало/победа/поражение — сигналы сетки.

**Обязательная визуальная обратная связь:**

| Событие | Отображение |
|---|---|
| Игрок получил урон | белая вспышка + мигание на время неуязвимости; красная вспышка по краям экрана и красный свет по всему автомату, тряска терминала [решение 2026-09-19] |
| Враг получил урон | белая вспышка 0,1 с, рябь спрайта, искры, тёплая вспышка по краям экрана |
| Урон | крупная пиксельная цифра: по врагам жёлто-белая, по игроку красная и крупнее |
| Заряжен чип | прицел на поле (клетки удара) и аура на персонаже по типу чипа; кассета наклонена, жёлтая полоса снизу; кольцо трекбола «дышит», дорожка платы гонит импульсы к кольцу |
| Цепочка отменена уроном | все не сработавшие кассеты сбрасывают номера очереди и трижды мерцают красным внутренним светом; неразрешённый текущий чип возвращается в своё гнездо |
| Выстрел чипа | тряска терминала, импульс по плате: кольцо → чип → экран |
| Телеграф атаки врага | подсветка клеток + поза/цвет врага |
| Прицел Canodron-а | маркер в клетке; красный при захвате |
| Снаряд или волна | простой меш (волна — клин, шар — сфера) |
| Удар меча | плоская «дуга» на поражённых клетках 0,15 с |
| Выбор чипа | состояния ячеек (§7.6) |
| Тап по трекболу сработает | кольцо трекбола горит жёлтым `#ffd45e` [решение 2026-09-20] |
| Удаление врага или игрока | сжатие и исчезновение 0,4 с |

---

## 15. Техническая архитектура

### 15.1. Принципы

1. **Симуляция отделена от отрисовки.** Код в `sim/` — чистый TypeScript без Three.js и DOM. Отрисовка только читает состояние симуляции. Поэтому логику можно тестировать и воспроизводить.
2. **Фиксированный шаг симуляции — 60 Гц** (как в MMBN на GBA). Отрисовка идёт с частотой экрана (60/90/120 Гц) и интерполирует позиции между тиками. Все длительности в конфиге задаются в секундах и переводятся в тики.
3. **Детерминизм:** весь случайный выбор (перемешивание папки, ИИ Spiker-а) идёт через один ГСЧ с сидом. Одинаковый сид + одинаковый ввод = одинаковый бой.
4. **Всё настраивается данными:** параметры в `config/tuning.ts`, чипы, враги и бои — в `data/`.
5. **Шина событий** (`hit`, `chipUsed`, `enemyDeleted`, `gaugeFull`…). На неё подписываются визуальные эффекты, HUD и (позже) звук.

### 15.2. Структура проекта

```
glorp-battle/
├─ index.html
├─ vite.config.ts
├─ src/
│  ├─ main.ts                 # точка входа, сборка модулей
│  ├─ config/tuning.ts        # ВСЕ настраиваемые параметры (§17)
│  ├─ core/
│  │  ├─ loop.ts              # фиксированный шаг + интерполяция
│  │  ├─ rng.ts               # ГСЧ с сидом (mulberry32)
│  │  ├─ events.ts            # шина событий
│  │  └─ input/               # swipe.ts, mouse.ts, keyboard.ts → InputCommands
│  ├─ sim/                    # без Three.js / DOM
│  │  ├─ world.ts             # состояние боя, тик
│  │  ├─ grid.ts              # клетки, занятость, территории
│  │  ├─ stateMachine.ts      # §11
│  │  ├─ player.ts            # передвижение, оглушение, неуязвимость
│  │  ├─ damage.ts            # применение урона, реестр попаданий
│  │  ├─ chips/
│  │  │  ├─ folder.ts         # экземпляры, перемешивание, добор
│  │  │  ├─ chipSystem.ts     # рука, заряд, общий cooldown, добор
│  │  │  ├─ selection.ts      # правило совместимости заряда
│  │  │  ├─ patterns.ts       # §6.4
│  │  │  └─ executor.ts       # исполнение одного чипа
│  │  ├─ enemies/
│  │  │  ├─ enemyBase.ts      # общий конечный автомат
│  │  │  ├─ mettik.ts
│  │  │  ├─ canodron.ts
│  │  │  └─ spiker.ts
│  │  └─ attacks/             # shockwave.ts, heatShot.ts, cannonShot.ts
│  ├─ data/
│  │  ├─ chips.ts             # §6.2
│  │  ├─ folders.ts           # §6.3
│  │  ├─ enemies.ts           # §8
│  │  └─ battles.ts           # §10.1
│  ├─ render/
│  │  ├─ scene.ts, camera.ts  # ортокамера, подгонка поля под экран
│  │  ├─ field.ts             # панели, подсветки
│  │  ├─ sprites.ts           # генерация текстур-заглушек
│  │  └─ fx.ts                # вспышки, трассеры, цифры урона
│  ├─ ui/
│  │  ├─ hud.ts
│  │  ├─ customScreen.ts
│  │  ├─ controls.ts          # кнопки Chip / Custom
│  │  ├─ screens.ts           # title, pause, defeat, result, complete
│  │  └─ styles.css
│  ├─ i18n/en.ts             # все строки интерфейса (§0.2)
│  ├─ audio/audio.ts          # заглушка: подписка на события (P2)
│  └─ debug/
│     ├─ debugPanel.ts        # lil-gui
│     └─ overlay.ts           # FPS, состояние, координаты
└─ tests/                     # Vitest
   ├─ selection.test.ts
   ├─ hand.test.ts
   ├─ patterns.test.ts
   ├─ gauge.test.ts
   └─ enemies.test.ts
```

### 15.3. Производительность

- Chrome (Android и десктоп): стабильно ≥ 60 FPS, цель — 120 FPS.
- `renderer.setPixelRatio(min(devicePixelRatio, 2))`, антиалиасинг включён.
- В игровом цикле нет выделений памяти: снаряды и эффекты берутся из пулов, геометрии и материалы переиспользуются.
- Отрисовка по `requestAnimationFrame`. Если кадр занял больше 250 мс (например, вкладка была в фоне), лишние тики симуляции отбрасываются.
- Боевая логика использует elapsed ticks фиксированной симуляции; длительности задаются в секундах, движение снарядов — в секундах на клетку. Render FPS 30/60/120 не меняет порядок фаз, число ударов и позиции симуляции [решение 2026-09-23].
- Размер canvas обновляется при `resize` и `orientationchange`, камера пересчитывается так, чтобы поле всегда помещалось.

### 15.4. ГСЧ и сиды

- `?seed=12345` в URL задаёт сид. Без параметра сид случайный и **выводится в отладочном оверлее**, чтобы бой можно было воспроизвести.
- Из основного сида выводятся отдельные потоки: перемешивание папки и ИИ врагов, чтобы изменение одного не сдвигало другое.

### 15.5. Отладочные инструменты (P0, развивать по ходу)

Включаются через `?debug=1`, клавишей `` ` ``/F2 или кнопкой DBG в правом нижнем углу (с 2026-09-18; левый нижний угол занимает клавиша паузы).

**Панель (lil-gui):**
- Все параметры из `tuning.ts` редактируются на лету, сгруппированы по разделам. Есть кнопки «Reset» и «Export JSON», значения сохраняются в `localStorage`.
- Session: папка отладочных боёв `basic` / `field` / `all` (перезагружает страницу), бой 1–4 (первые четыре встречи), шаг забега, перезапуск боя.
- Сид: показать, задать, перезапустить с ним.
- Масштаб времени: 0,25× / 0,5× / 1× / 2×; пауза и покадровый шаг.
- Читы:
  - бессмертие;
  - убить всех врагов;
  - установить HP игрока;
  - выдать любой чип в очередь.
- ИИ врагов: вкл/выкл, принудительная атака.

**Оверлей:**
- FPS, мс на кадр, число тиков;
- текущее состояние автомата; для каждого врага — фаза `INTENTION/LOCK/COUNTER/STRIKE/RECOVERY` и оставшееся время в миллисекундах;
- координаты клеток, подсветка занятости;
- содержимое папки, руки, заряда и очереди добора, фаза `selecting` / `committed` / `waiting`;
- прогресс общего cooldown, число потраченных слотов, сид.

**Логи:** включаемый журнал событий шины в консоль.

**Параметры URL:** `?debug=1&seed=…&battle=3&encounter=e1&folder=basic|field|all&god=1&timescale=0.5`. `battle=N` (1–4) или `encounter=<id>` (`n1`…`n14`, `e1`…`e4`, `boss`) пропускает титул и открывает бой с папкой из `folder=` (по умолчанию `basic`).

### 15.6. Звук (P2, но с заделом)

- Модуль `audio.ts` подписан на шину событий. Для MVP это заглушки.
- Позже добавим: выстрел, заряд готов, удар меча, попадание, взрыв, удаление врага, выбор, снятие и восстановление чипа, музыку боя.
- Звук включается после первого касания (политика автовоспроизведения Chrome). Регулятор громкости — в паузе.

### 15.7. Тесты (Vitest)

Обязательно покрыть чистую логику:

- Правило совместимости: одно имя / один код / `*` / смешанные недопустимые наборы / лимит 5.
- Рука:
  - перемешивание по сиду детерминировано;
  - добор до 5/10/15;
  - сохранение остатка руки;
  - поведение при пустой папке.
- Заряд: порядок, снятие до запуска, фиксация первой атакой и отдельное нажатие Attack для каждого чипа.
- Урон игроку до hit frame: текущий чип возвращается в исходный слот, весь заряд отменяется, все не сработавшие кассеты получают сигнал. После hit frame текущий эффект сохраняется и отменяется только хвост.
- Общий cooldown: стартует с первым чипом, переживает прерывание, не открывает руку до завершения заряда и одновременно пополняет только потраченные слоты.
- Пустая очередь добора: использованные чипы перемешиваются обратно (`drawReshuffled`), готовый слот корректно получает чип.
- Шаблоны атак: поражаемые клетки для каждой позиции игрока.
- Урон: один раз за атаку, неуязвимость, оглушение прерывает чип.
- ИИ: несколько Mettik атакуют по очереди; Canodron не стреляет, если игрок ушёл из линии; Spiker не телепортируется в занятую клетку.
- Забег: 10 шагов, элитные на 5 и 8, босс на 10, лечение после 3/6/9; случайная папка соблюдает правила §6.3 и детерминирована по сиду.

### 15.8. Сборка

- `npm run dev` — сервер разработки, доступный в локальной сети для теста на телефоне (`--host`).
- `npm run build` — статическая сборка, можно выложить на любой статический хостинг.
- `npm test` — тесты.
- `npm run typecheck` — `tsc --noEmit`, строгий режим (`strict: true`).

---

## 16. Приоритеты и этапы

| Этап | Приоритет | Содержимое | Готово, когда |
|---|---|---|---|
| M0 | P0 | Каркас: Vite + TS + Three, цикл с фиксированным шагом, конфиг, отладочные панель и оверлей, сид | Пустое поле 3×6 отрисовано, FPS в оверлее ≥ 60 |
| M1 | P0 | Сетка, камера, игрок, свайпы, мышь, клавиатура | Игрок ходит по своей территории 3×3 и не выходит за неё |
| M2 | P0 | Урон, Mettik, волна, телеграф, удаление (Buster был здесь, удалён 2026-09-18) | От волны можно уклониться |
| M3 | P0 | Кастом-шкала, кастом-экран, папка, рука, выбор, OK, «BATTLE START!» | Сделано в M3, **снято 2026-09-18**: шкала и кастом-экран удалены, выбор ушёл в реальное время (§5, §7) |
| M4 | P0 | Использование чипов: 5 чипов P0 | Выбранные чипы срабатывают по порядку; использованные исчезают из пула |
| M5 | P0 | ADD | Сделано вместе с M3, **снято 2026-09-18** вместе с кастом-экраном |
| M6 | P1 | Canodron, Spiker, очерёдность атак нескольких Mettik (сделана в M2) | Все три врага ведут себя по-разному |
| M7 | P1 | Цепочка из 4 боёв, результат, поражение, повтор, пауза, финал | Всю цепочку можно пройти от начала до конца |
| M8 | P1 | Чипы P1 (LongSword, Shotgun, MiniBomb), отладочная папка P1 | Чипы работают по §6.4 — **логика и эффекты сделаны вместе с M4**, осталось проверить папку P1 в игре |
| M9 | P2 | Звук, локализация, уровень боя, полировка | — |

**Статус (2026-09-16):** M0–M7 готовы; M5 сделан вместе с M3, логика чипов P1 (M8) — вместе с M4. Retry перезапускает бой с новым перемешиванием папки (сид попытки выводится из сида сессии, номера боя и номера попытки) [решение].

**2026-09-18:** забег упрощён до линейного (§10), Buster, награды, наследие и Punchy удалены, чипы и вирусы сверены с MMBN3, добавлены три стартовые папки (§6.3) [решение 2026-09-18].

**После M7 разработку останавливаем и оцениваем бой.** До этой оценки P2-системы не делаем.

**Далее (2026-09-17):** этапы T1–T3 — физический терминал, см. [TERMINAL.md §11](TERMINAL.md). Новые параметры группы `terminal` — [TERMINAL.md §17](TERMINAL.md); переносятся в §17 по мере реализации.

---

## 17. Таблица настраиваемых параметров (`config/tuning.ts`)

Группы `battleVisual` и `terminal` описаны в [BATTLE_VISUAL.md §9](BATTLE_VISUAL.md) и [TERMINAL.md §17](TERMINAL.md). Состав папок, правила случайной папки, множители уровней и шаги забега — данные и константы кода (§6.3, §8.5, §10), не tuning.

| Ключ | Значение | Ед. | Метка |
|---|---|---|---|
| `SIM_HZ` | 60 | Гц | [MMBN6] |
| `MAX_FRAME_TIME` | 0.25 | с | длинный кадр отбрасывает лишние тики (§15.3) |
| `TIME_SCALE` | 1 | × | отладка |
| `PLAYER_MAX_HP` | 100 | HP | [MMBN3] |
| `PLAYER_START_X` / `PLAYER_START_Y` | 1 / 4 | клетка | [MMBN6] |
| `CELL_MOVE_TIME` | 0.2 | с/клетка | базовый ритм игрока [решение 2026-09-23] |
| `PLAYER_FLINCH_TIME` | 0.4 | с | [оценка] |
| `PLAYER_IFRAMES` | 2.0 | с | [оценка] |
| `PANEL_RESTORE_TIME` / `STEAL_RESTORE_TIME` | 10 / 15 | с | [оценка]; `STEAL_RESTORE_TIME` — для AreaGrab и PanlGrab |
| `ROCK_HP` | 100 | HP | [оценка] |
| `GAUGE_FILL_TIME` | 8.0 | с | не используется: шкала удалена 2026-09-18 |
| `HAND_SIZE` | 5 | | [решение 2026-09-18] |
| `DRAW_PREVIEW` | 3 | | [оценка] |
| `CHIP_STARTUP_*` / `CHIP_RECOVERY_*` | VULCAN 0.1/0.1; CANNON 0.1/0.15; SWORD 0.15/0.2; BOMB 0.2/0.2; RECOVER 0.1/0.15; FIELD 0.15/0.15 | с | [решение 2026-09-23] |
| `HAND_REFILL_COOLDOWN` | 4.0 | с | [оценка 2026-09-22] |
| `PARALYZE_TIME` | 1.5 | с | [оценка] |
| `INVIS_TIME` | 3.0 | с | [оценка] |
| `ACTION_BUFFER_TIME` | 0.1 | с | один ранний Attack [решение 2026-09-23] |
| `CELL_TRAVEL_TIME` / `FAST_CELL_TRAVEL_TIME` / `SLOW_CELL_TRAVEL_TIME` | 0.2 / 0.15 / 0.3 | с/клетка | [решение 2026-09-23] |
| `COUNTER_STAGGER_TIME` | 0.5 | с | общий stagger после Counter [оценка] |
| `INTENTION_TIME` / `LOCK_TIME` / `COUNTER_TIME` / `STRIKE_TIME` / `RECOVERY_TIME` / `MOVE_TIME` (fast) | 0.2 / 0.15 / 0.15 / 0.1 / 0.25 / 0.15 | с | Spiker, Hopzap, Rattik, Finnik [решение 2026-09-23] |
| те же ключи (standard) | 0.3 / 0.2 / 0.18 / 0.1 / 0.3 / 0.2 | с | Mettik, Canodron [решение 2026-09-23] |
| те же ключи (heavy) | 0.4 / 0.25 / 0.2 / 0.15 / 0.4 / 0.3 | с | Bladdy, Helmhead, Monolith [решение 2026-09-23] |
| `TUT_HINT_PULSE` / `TUT_HINT_SEG` / `TUT_HINT_LINE` | 2 / 4 / 8 | с | лесенка подсказок туториала (§10.5), бездействие с начала такта [решение 2026-09-19] |
| `MET_HP` / `MET_DMG` | 40 / 10 | | [MMBN3] |
| `CANO_HP` / `CANO_DMG` | 60 / 10 | | [MMBN3] |
| `CANO_CURSOR_STEP` | 0.15 | с/клетка | [оценка] |
| `SPK_HP` / `SPK_DMG` | 90 / 30 | | [MMBN3] |
| `SPK_WARPS_MIN` / `SPK_WARPS_MAX` | 2 / 4 | | [оценка] |
| `HOP_HP` / `HOP_DMG` | 40 / 15 | | [MMBN3] |
| `HOP_PARALYZE` | 1.0 | с | [оценка] |
| `BLD_HP` / `BLD_DMG` | 90 / 30 | | [MMBN3] |
| `RAT_HP` / `RAT_DMG` | 40 / 20 | | [MMBN3] |
| `HELM_HP` / `HELM_DMG` | 80 / 60 | | [MMBN3] |
| `HELM_CLOSED` | 2.0 | с | [оценка] |
| `FIN_HP` / `FIN_DMG` | 90 / 30 | | [MMBN3] |
| `FIN_DASH_STEP` | 0.08 | с/клетка | движение внутри Strike [оценка] |
| `MONO_HP` | 400 | HP | [решение 2026-09-18] |
| `MONO_ATTACK_INTERVAL` | 2.5 | с | [оценка] |
| `MONO_ROCK_DMG` / `MONO_ROCKS` / `MONO_ROCKS_RAGE` | 40 / 3 / 5 | | [оценка] |
| `MONO_WAVE_DMG` / `MONO_RAGE_SPEED` | 50 / 1.25 | | [оценка] |
| `HIT_FLASH` | 0.1 | с | вспышка при попадании (игрок и враги) |
| `IFRAME_BLINK_HZ` | 15 | Гц | мигание неуязвимого игрока |
| `CANNON_TRACER_TIME` | 0.18 | с | трассер пушек |
| `SLASH_TIME` | 0.15 | с | дуга меча |
| `EXPLOSION_TIME` | 0.3 | с | взрыв MiniBomb |
| `HEAL_FX_TIME` | 0.5 | с | кольцо лечения |
| `WARP_FX_TIME` | 0.15 | с | вспышка телепорта Spiker |
| `DAMAGE_NUMBER_TIME` | 0.6 | с | |
| `DELETE_ANIM_TIME` | 0.4 | с | |
| `INTRO_TIME` | 0.8 | с | появление врагов и раздача руки перед боем |
| `RESULT_DELAY_WIN` | 1.2 | с | сигнал победы до экрана PATH / COMPLETE |
| `RESULT_DELAY_LOSE` | 1.0 | с | удаление игрока до экрана GAME_OVER |
| `SWIPE_MIN_PX` | 24 | px | [оценка] |
| `SWIPE_REARM_TIME` | 0.07 | с | остановка пальца, после которой новый рывок даёт шаг [оценка] |
| `HOLD_REPEAT_DELAY` | 0.35 | с | [оценка] |
| `HOLD_REPEAT` | 0.2 | с | [оценка] |
| `TAP_MAX_TIME` (группа `terminal`) | 0.35 | с | тап по трекболу = чип [оценка] |

---

## 18. Критерии приёмки MVP

Всё ниже должно работать **в Chrome на Android (портрет) и в Chrome на десктопе**:

1. С титульного экрана тремя кнопками запускается забег с папкой Basic, Field или Random; рука из пяти чипов стоит в гнёздах с первого кадра, отдельного экрана выбора нет.
2. Свайпом (мышью на десктопе) игрок ходит по своей территории 3×3 и не может выйти за её пределы.
3. Один рывок по трекболу сдвигает игрока ровно на одну клетку; каждый тап по трекболу запускает ровно один чип заряда.
4. От каждой атаки врага можно уклониться, сменив клетку после появления телеграфа.
5. Бой ни разу не останавливается после `BATTLE_INTRO` — ни для выбора чипов, ни для восстановления слотов.
6. В руке 5 чипов из перемешанной по сиду папки; невыбранные чипы сохраняются и входят в следующую активную руку.
7. Совместимые чипы (одно имя или один код) набираются в заряд, несовместимые гаснут в том же кадре; до первой атаки состав и порядок можно менять.
8. Первая атака фиксирует заряд; чипы запускаются строго по порядку, по одному на нажатие. Следующая рука активируется только после завершения заряда и четырёхсекундного cooldown.
9. Использованные чипы возвращаются только через перемешивание, когда очередь добора пуста.
10. ~~ADD~~ — снято 2026-09-18 вместе с кастом-экраном.
11. Mettik, Canodron и Spiker ведут себя по-разному, как в §8; несколько Mettik атакуют по очереди.
12. Все 10 шагов забега проходятся подряд, HP переносится и полностью восстанавливается после шагов 3, 6 и 9.
13. Поражение или ABANDON → GAME_OVER → титул; RETRY нет.
14. Пауза включается вручную и при сворачивании вкладки.
15. FPS ≥ 60 на целевом телефоне, в оверлее нет просадок ниже 55 в течение боя.
16. Отладочная панель позволяет менять любые параметры §17 на лету, выбирать бой и задавать сид.
17. Тесты §15.7 проходят, `tsc` без ошибок.
18. Весь текст в игре на английском и берётся из `i18n/en.ts`; строк прямо в коде интерфейса нет.
19. Прогрессии между забегами, сохранений и внешних систем нет.

---

## 19. Не делаем в MVP

Оверворлд, сюжет, NPC, экономика, магазины, ремонт и события на пути, коллекция и библиотека чипов, ручной редактор папки, постоянная прогрессия и наследие, награды и рост папки, выбор пути, Navi-чипы, Program Advance, стихии и слабости, стихийные панели, стили, мультиплеер, финальная графика. Звук и локализация — P2. [решение 2026-09-17: босс, панели, захват, паралич есть; решение 2026-09-18: награды, наследие и выбор пути удалены]

---

## 20. Открытые вопросы (решаем на плейтесте)

1. ~~Buster~~ — закрыт: Buster удалён 2026-09-18 (§4).
2. **Тайминги врагов и чипов** помечены [оценка]: точных покадровых данных в открытых источниках не нашли. Подбираем на глаз по видео MMBN3/MMBN6 через отладочную панель.
3. ~~HP Canodron-а~~ — закрыт: 60 по MMBN3.
4. ~~Spiker~~ — закрыт: Spikey есть в MMBN3 (90 / 30).
5. ~~Клавиши кастом-экрана~~ — закрыт: кастом-экран удалён 2026-09-18.
6. **Уровень боя** (оценка 1–10/S, как Busting Level в серии) — P2.

---

## Источники

Текущие (с 2026-09-18):

- [MMKB — List of MMBN3 Battle Chips](https://megaman.miraheze.org/wiki/List_of_Mega_Man_Battle_Network_3_Battle_Chips) — урон, коды и имена чипов (§6.2)
- [MMKB — Mettaur](https://megaman.miraheze.org/wiki/Mettaur), [Canodumb](https://megaman.miraheze.org/wiki/Canodumb), [Spikey](https://megaman.miraheze.org/wiki/Spikey) и страницы других вирусов — HP и урон по играм серии; берём строку MMBN3 (§8)
- [LP Archive — MMBN2 Virus roundup](https://lparchive.org/Mega-Man-Battle-Network-2/Update%2060/) — поведение Mettaur и Canodumb

История (значения до 2026-09-18, когда ориентиром была первая игра серии):

- [GameFAQs — MMBN Guide: Battle Chips](https://gamefaqs.gamespot.com/gba/457634-mega-man-battle-network/faqs/49840/battle-chips), [Virus Data](https://gamefaqs.gamespot.com/gba/457634-mega-man-battle-network/faqs/49840/virus-data), [Basic Controls](https://gamefaqs.gamespot.com/gba/457634-mega-man-battle-network/faqs/49840/basic-controls), [Virus Busting](https://gamefaqs.gamespot.com/gba/457634-mega-man-battle-network/faqs/49840/virus-busting)
- [MMKB — Custom Screen](https://megaman.miraheze.org/wiki/Custom_Screen), [Custom Gauge](https://megaman.miraheze.org/wiki/Custom_Gauge), [Mega Buster (MegaMan.EXE)](https://megaman.miraheze.org/wiki/Mega_Buster_(MegaMan.EXE)) — системы, которые удалены
- [MMKB — List of MMBN Battle Chips](https://megaman.miraheze.org/wiki/List_of_Mega_Man_Battle_Network_Battle_Chips)
