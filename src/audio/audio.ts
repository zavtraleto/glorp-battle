// Audio hooks (GDD §15.6, P2). Will subscribe to the event bus; intentionally empty for now.

export function initAudio(): void {
  // P2: unlock AudioContext on first user gesture and map bus events to sounds.
}
